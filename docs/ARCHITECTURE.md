# ADIA Architecture

## System Overview

ADIA is planned as a full-stack TypeScript application with a Next.js App Router frontend, Supabase backend services, deterministic analysis packages, and server-side LLM insight generation.

The system is intentionally analysis-first. It ingests deployment evidence, stores it, normalizes it, detects risk with deterministic rules, then uses an LLM to summarize and recommend with evidence links.

## Text Architecture Diagram

```text
GitHub Actions / Terraform / Logs
        |
        v
Ingestion API
        |
        v
Supabase Postgres
        |
        v
Terraform plan parser + IaC scanner parser
        |
        v
Deterministic anomaly engine
        |
        v
LLM insight service
        |
        v
Real-time Tailwind dashboard
```

## Planned Data Flow

1. A deployment run starts in GitHub Actions or is loaded from a local/demo fixture.
2. The ingestion API receives run metadata, Terraform plan JSON, Checkov JSON, and relevant logs.
3. Supabase stores raw evidence and normalized records.
4. Parser modules summarize Terraform and IaC scan data.
5. The anomaly engine produces deterministic findings.
6. The LLM insight service receives redacted, structured evidence and returns validated JSON.
7. The dashboard subscribes to project/run updates and renders health, risks, anomalies, insights, and recommendations.

## Planned Ingestion Flow

Initial ingestion sources:

- GitHub Actions workflow metadata.
- Terraform plan JSON generated by `terraform show -json`.
- Checkov JSON output.
- Log snippets from deployment jobs.
- Manual/demo fixtures for local development.

The ingestion API will validate payload shape before persistence. Phase 0 only creates fixture directories and a placeholder script.

## Planned Analysis Pipeline

The deterministic pipeline will run before any LLM step:

1. Parse Terraform plan JSON into normalized resource changes.
2. Parse IaC scan findings into normalized security findings.
3. Detect high-risk infrastructure changes.
4. Detect anomalies in status, duration, failure pattern, resource blast radius, and exposure changes.
5. Produce structured evidence records for dashboard and LLM use.

## Planned LLM Insight Pipeline

The LLM pipeline will be server-side only:

1. Gather deterministic findings and evidence.
2. Redact secrets and sensitive values.
3. Build a compact structured prompt.
4. Request structured JSON output.
5. Validate the response schema.
6. Store insight and recommendation records.
7. Display output with evidence links.

The LLM is not allowed to execute infrastructure commands.

## Planned Supabase Role

Supabase will provide:

- Auth.
- Postgres.
- Row-level security.
- Realtime subscriptions.
- Edge functions only if they fit future ingestion or processing needs.

Phase 1 will define schema and RLS. Phase 0 only creates Supabase directories and a seed placeholder.

## Phase 1 Schema Foundation

Phase 1 adds the planned Supabase data foundation behind the architecture pipeline. Organizations define tenant boundaries, projects group deployment visibility, and deployment runs anchor Terraform plan evidence, IaC scan findings, anomalies, insights, recommendations, and evidence links.

The schema is intentionally advisory and evidence-first. It does not execute deployments, run Terraform, call LLM providers, or connect the dashboard to live Supabase data. Those behaviors remain outside Phase 1.

## Planned Terraform Role

Terraform is a source of evidence and optional demo infrastructure in this project. ADIA will parse Terraform plan output, but the MVP will not run `terraform apply` from the UI.

The `infra/` directory contains safe placeholders only in Phase 0. Providers and real resources will be added intentionally in later phases.

## Planned Frontend Role

The frontend will be a Tailwind CSS dashboard built with Next.js App Router. It will show deployment health, Terraform risk, anomalies, AI insights, and recommendations.

Phase 0 includes a static landing page and dashboard placeholder. Supabase Realtime will be added later.
